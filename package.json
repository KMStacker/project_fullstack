{
  "name": "project_fullstack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm test --prefix backend && npm test --prefix frontend && npm run test:e2e",
    "test:e2e": "playwright test",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend",
    "create-admin": "npm run create-admin --prefix backend",
    "seed-db": "npm run seed-db --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "concurrently": "^9.2.0",
    "dotenv": "^17.4.2"
  }
}
